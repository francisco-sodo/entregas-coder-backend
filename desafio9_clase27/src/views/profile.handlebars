<div>
  <h1>Tu perfil (sin datos sensibles)</h1>
  <p>{{user.name}}</p>
  <p>{{user.email}}</p>
  <p>{{user.age}}</p>
  <div><p>Carrito:</p>{{user.cart}}</div>
</div>

<div>
  {{! <button>Log Out</button> }}
  <a href="/products">Volver a Productos</a>
</div>